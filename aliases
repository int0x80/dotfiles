# -----------------------------------------------------------
# leaner alias file
# -----------------------------------------------------------

function urldecode() { : "${*//+/ }"; echo -e "${_//%/\\x}"; }
